package com.manosprojects.themoviedb.domain.usecase

import org.junit.Test

class MarkMovieAsFavouriteUCTest {

    @Test
    fun `when execute(movieId, isFavourite) is subscribed to, then verify interactions`() {

    }
}